SELECT 
    c.CategoryName,
    d.DishName,
    d.Price,
    CASE 
        WHEN d.IsSpicy = 1 THEN 'Yes'
        ELSE 'No'
    END as IsSpicy,
    CASE 
        WHEN d.IsVegetarian = 1 THEN 'Yes'
        ELSE 'No'
    END as IsVegetarian
FROM Categories c
JOIN Dishes d ON c.CategoryID = d.CategoryID
ORDER BY c.CategoryName, d.Price DESC; 